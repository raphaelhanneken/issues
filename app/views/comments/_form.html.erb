<%= minimal_form_for @comment, url: report_comments_path(@report), method: 'post' do |f| %>
  <%= f.input :content, placeholder: 'Comment', as: :text, input_html: { rows: '4' } %>
  <p class="text-xs-right">
    <%= f.button :submit, value: 'Comment', class: 'btn-success' %>
  </p>
<% end %>

<hr>
