var ele = $('#edit-labels #label-<%= escape_javascript(@label.id.to_s) %>');

ele.attr('href', "<%= escape_javascript(remove_label_from_report_path(@report, @label)) %>");
ele.parent().removeClass('add-label').addClass('remove-label');

$('body').append("<%= escape_javascript(render('shared/flash_messages')) %>");
